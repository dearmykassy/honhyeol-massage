# 혼혈마사지

Template5 화면 구조를 사용한 Next.js 16 정적 출장마사지 안내 사이트입니다. 운영 도메인은 `https://honhyul.kr`입니다.

## 페이지 구성

- 지역 페이지: 1,291개
- 고정 페이지: 홈, 지역 찾기, 가격, 이용 안내, 공지, 블로그 목록 6개
- 블로그 글: 2개
- sitemap 합계: 1,299개 URL
- RSS 2.0: 실제 블로그 글 2개의 canonical URL, 실제 발행일과 전체 본문을 `/rss.xml`로 제공
- 지역 검색: 시·군·구, 동·읍·면, 지역 별칭을 상세 경로에 연결
- 메타데이터: 모든 1,299개 URL에 title, keywords, description, canonical, Open Graph, Twitter 적용
- 공개 상태: `https://honhyul.kr`, production canonical + `index,follow`, robots 전체 허용

## 광역 상세 페이지 기준

광역 상세는 아래 코드 한 줄로만 분류합니다.

```ts
node.kind === "root" || /시$/u.test(node.displayName)
```

결과는 정확히 41개입니다.

- 시작 권역 11개: 서울, 인천, 경기, 천안, 아산, 대전, 대구, 구미, 포항, 부산, 제주
- 경기 시 단위 28개: 고양시, 과천시, 광명시, 광주시, 구리시, 군포시, 김포시, 남양주시, 동두천시, 부천시, 성남시, 수원시, 시흥시, 안산시, 안성시, 안양시, 양주시, 여주시, 오산시, 용인시, 의왕시, 의정부시, 이천시, 파주시, 평택시, 포천시, 하남시, 화성시
- 제주 시 단위 2개: 제주시, 서귀포시

구·군·동·읍·면은 광역 상세에 포함하지 않으며 나머지 1,250개 경로는 주소 계층과 실제 지역 연결을 중심으로 한 세부 지역 형식을 사용합니다. 세부 지역 형식은 주소 명칭, 상위 주소, 같은 상위 주소의 다른 항목, 전화 준비, 일정, 코스, 2인 프로그램, 결제, 위생, 첫 문의, 변경 확인의 11개 섹션과 22개 문단으로 고정합니다. 광역 페이지에는 하위 지역, 서비스 주소 확인 범위, 운영 순서, 코스·시간, 처음 이용할 때의 순서, 전화 전 준비, 현장 결제, 변경 재확인만 둡니다. 실제 지도가 없으므로 위치 지도를 만들지 않으며 매장, 인기 장소, 후기, 지역별 이용량을 주장하지 않습니다.

## 확인된 운영 정보

- 전화: 0508-202-3906
- 접수: 365일 24시간
- 결제: 예약금·선입금 없이 이용 뒤 현장 정산
- 카드: 현장용 무선 단말기 사용 가능
- 프로그램: 공개 가격표 5개 코스, 14개 시간·금액 항목, 커플·부부 2인 동시 프로그램
- 비품: 일회용 비품 사용, 관리 전후 소독

특정 지역·주소의 이용 가능 여부와 시작 시각은 페이지에서 확정하지 않고 상세 주소와 희망 시간을 전달한 전화에서 확인합니다.

## 검증 명령

```bash
pnpm install --frozen-lockfile
pnpm test
pnpm audit:copy
pnpm typecheck
pnpm lint
pnpm build
pnpm audit:build
```

`pnpm audit:copy`는 1,291개 지역의 제목·설명·H1·문단·정규화된 페이지 서명을 검사하고, 로컬에 있는 기존 플랫폼의 고객 문장과 정확히 겹치는 긴 문자열을 비교합니다. `pnpm audit:build`는 `out/`의 1,299개 HTML, sitemap, robots, 메타 필드, 다른 브랜드 잔여 문자열과 이미지 경로를 확인합니다.

## 이미지 계약

이미지 경로는 Template4 시기의 namespace를 그대로 유지합니다. Template5 전환 자체는 UI만 바꿨으며, 이후 2026-08-17 지역 배너 피드백에 따라 `044`, `066`–`130`의 정확히 66개 자산만 별도 v2 승인 체인으로 교체했습니다. 나머지 지역 자산과 메인·블로그 이미지는 그대로입니다.

- 지역 원본: `public/images/honhyeol-template4/regional-originals/lane-{a..j}/hym-t4-rgn-{001..130}-v1.png`
- 지역 파생본: `public/assets/honhyeol-massage/template4-regional/{assetId}/{desktop,tablet,mobile}.webp`
- 지역 배정: 사진 130개를 1,291개 경로에 최대 10회 사용하며 부모·자식과 같은 부모 아래 형제끼리 같은 사진을 쓰지 않음
- 홈·블로그: `public/images/honhyeol-template4/home/`, `public/images/honhyeol-template4/blog/`
- 브랜드 마크: 건마에반하다의 하트와 구분되는 투명 배경 원형 궤도형 마크를 `public/images/honhyeol-template5/brand/`에 보관한다. 헤더·푸터와 32·192·512 아이콘은 같은 승인 원본을 사용한다.
- 홈 첫 8개 권역 카드는 사용자 승인에 따라 마사지러브의 도시·랜드마크 WebP를 바이트 그대로 재사용하며, 공개 provenance에서 저자와 라이선스를 제공한다. 지역 상세페이지 배너의 1,291개 배정은 변경하지 않는다.

이미지 승인·파생본 생성이 끝나기 전에는 배포 완료로 기록하지 않습니다.

지역 배너 v2는 실제 국적이나 혈통을 외모로 판정하는 자료가 아닙니다. 생성 프롬프트를 성인 한국 여성의 현대 한국 패션 화보 방향으로 명시하고, 완전 착의·비노골적 포즈·충분한 얼굴 노출·실제 거울과 반사·모바일 중앙 크롭을 검수합니다. 기존 원본과 1차 승인 문서는 로컬 감사 이력으로 보존하며, 메인 홈 배너 `public/images/honhyeol-template4/home/hero-mirror.webp`는 변경하지 않습니다.

## 공개 배포 체크

metadata origin, canonical, Open Graph URL, sitemap, RSS와 robots는 `https://honhyul.kr`로 통일합니다. 도메인이나 배포 설정을 바꿀 때는 전체 검증을 다시 실행하고 운영 HTTPS, canonical, robots, sitemap·RSS 호스트를 함께 확인합니다. 네이버 서치어드바이저 온보딩에서는 RSS를 제출하고 수집 주기를 `빠르게`로 설정합니다.
